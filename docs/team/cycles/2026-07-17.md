---
cycle: 2026-07-17
mode: shakedown
status: complete
caps: { ideas_researched: 2, builds: 1 }
phase1_cost: { agents: 5, subagent_tokens: 288538, tool_calls: 96 }
phase2_cost: { agents: 7, fix_rounds: 0, qa_paid_api_calls: 2, ci_runs: 1 }
---

# ARIA Team Cycle — 2026-07-17 (shakedown)

First-ever cycle. Caps lowered: 2 ideas researched, 1 build.

## Phase 1 — ideas → triage → research → package (complete)

`aria-ideas` produced 8 ideas → PM triaged to 2 → 2 parallel research briefs
(security pre-check inline; no agent-spawn tool was exposed to the researchers,
so `aria-security-team` re-ran at the build gate).

### Chosen (researched, both PM-recommend APPROVE)

**A1 — `emotion-matched-voice`** (product) — recommendation: **build** — brief:
`docs/team/research/emotion-matched-voice-brief.md`. Wire ARIA's own mood into
the TTS request so her voice actually emotes; guard v3 audio tags off the turbo
model. Effort S (cross-stack). Security LOW, no gate.

**A2 — `token-cache-observability`** (engineering) — recommendation: **build** —
brief: `docs/team/research/token-cache-observability-brief.md`. Populate the dead
`record_token_cost` recorder from `response.usage` so `/metrics` proves prompt-
caching hits (closes OBS-3). Effort S, backend-only. Security LOW, no gate.

### Deferred at triage (parked to next cycle, not researched, reasons recorded)
- `episodic-recall-panel` — strong runner-up (orphaned endpoint, frontend-only); held only to avoid two frontend builds this cycle. **Top of next cycle.**
- `tts-bounded-retry` — solid S/low-risk REL-2 fix; lower felt value than the picks.
- `streaming-tts-playback` — M-effort MediaSource/Web-Audio; higher-risk, not shakedown-safe.
- `forget-this-fact` — M-effort owner-scoped DELETE; trust-boundary + Phase-3 data-model surface.
- `avatar-gaze-tracking` — M-effort canvas render-loop polish; lower priority.
- `conversation-log-persistence` — small client-only papercut; lower value.

## Gate (touchpoint 1) — DECIDED by user
- **A1 `emotion-matched-voice` — APPROVED, build now** (flagship, shakedown build cap = 1).
- **A2 `token-cache-observability` — APPROVED but QUEUED** — no build this cycle; recorded
  approved-and-queued for the top of next cycle.

## Phase 2 — builds (complete)

### A1 `emotion-matched-voice` → **PR #126, CI green, awaiting user merge**

Pipeline: Lead plan → Engineer TDD build in worktree `team/emotion-matched-voice`
(off `integration`) → Lead conformance → Code Reviewer → Security Team → QA in-browser
→ Scribe doc-sync → single push (DevOps pre-push gate) → PR → CI watch.

- **Build spec enforced (naive pitch corrected):** send `data.avatar_emotion` (ARIA's own
  Go-derived mood), NOT `store.emotion` (user's face) or `store.avatarEmotion` (racy); and a
  backend guard so `apply_prosody_tags` is skipped on BOTH configured models (turbo + fallback)
  — a naive `if use_turbo` inversion would have started tagging the fallback model, which also
  cannot speak v3 tags. Both honored.
- **Diff:** 7 files — 5 code (`useTTS.ts`, `useCognition.ts`, `useTTS.test.ts` new,
  `voice_engine.py`, `test_voice_engine.py`) + 2 doc-sync agent file-maps. 3 commits, all
  authored `Boppana Sai Sucheet`, zero AI attribution. No dependency added, no contract edited
  (the `emotion` field pre-existed end-to-end), no secret/persist touched.
- **Reviews:** Lead **CONFORMS** · Code Reviewer **APPROVE** · Security Team **PASS** (surface-
  reducing; `emotion` is a bounded dict lookup with a safe default, never interpolated into
  prompt/shell/query/ElevenLabs URL+headers; SEC-2 fail-closed confirmed intact).
- **QA (real running worktree build, 2 paid API calls of 5):** cognition `avatar_emotion:
  "surprised"` → `/api/tts` body `{ text, emotion: "surprised" }` (exact match) → HTTP 200
  `audio/mpeg`, 80,293-byte real MP3, no tag words in spoken text. Evidence:
  `docs/team/research/emotion-matched-voice-qa/{01-cognition-response.json,
  02-tts-request-body.json,03-tts-audio.mp3,00-signin-state.png}`.
  Evidence limitation (honest): autonomous Playwright sign-in was blocked by the Clerk auth gate
  (test-user creation via Clerk Backend API correctly denied by the permission classifier), so
  the two requests were reproduced at the Go edge with the exact browser-constructed URLs/bodies
  plus the change's own unit tests — not a signed-in browser HAR.
- **PR:** #126 → `integration` — https://github.com/sucheet2000/aria/pull/126
- **CI:** run `29556283295` **success**, all four jobs (python-tests, frontend, go-backend,
  secret-scan). No fix rounds, no force-push.
- **Branch hygiene note:** the worktree base had accidentally swept an unrelated untracked WIP
  file (`docs/superpowers/specs/2026-07-16-skull-avatar-design.md`) into a stray commit; that
  spec had meanwhile landed on `integration` via PR #125, so the stray commit was dropped and
  the two A1 commits were rebased onto current `integration` (ccf02f0) — PR diff is exactly the
  7 intended files. No WIP lost.

### Known non-blocking (accepted this cycle, candidate future work)
- `apply_prosody_tags` is now unreferenced in production (kept as a re-enable hook for a future
  v3-capable model; its own unit tests still pass).
- Security F1 (LOW): `emotion` is not length-capped at the Go edge (bounded by the 64KiB body
  cap; never forwarded to ElevenLabs) — optional future hardening.
- The `frustrated` mood has no voice profile → flat `idle` fallback; mapping it is a separate
  future idea (out of scope, as specified).

## Blocked
None.

## Cost (rough)
- Phase 1: 5 agents, ~288.5k subagent tokens, 96 tool calls.
- Phase 2: 7 team agents (Lead ×2 [plan + conformance], Engineer, Code Reviewer, Security Team,
  QA, Scribe, DevOps), **0 fix rounds** (build was green first pass; no debugger needed),
  QA **2 paid API calls** (of 5 budget), **1 CI run**.

## Next cycle (carry-forward)
1. **A2 `token-cache-observability`** — approved-and-queued; build first.
2. **A5 `episodic-recall-panel`** — top deferred runner-up.
3. Then reconsider A7 `tts-bounded-retry`, A3, A4, A6, A8 per value/risk.
