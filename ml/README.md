# ML Models

Training stubs for ARIA's emotion and gesture classification models.

## Emotion Model

See `emotion/train.py` for the training pipeline scaffold.

## Gesture Model

See `gesture/train.py` for the training pipeline scaffold.
